import SwiftUI

struct SettingsView: View {
    var body: some View {
        NavigationView {
            List {
                Section {
                    Text("Settings")
                }
            }
            .navigationTitle("Settings")
        }
    }
} 